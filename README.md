# TelBotRLTVideo

Telegram-бот для аналитики по видео с использованием естественного языка. Позволяет получать статистику по видео на основе текстовых запросов.

## 📋 Оглавление
- [Возможности](#-возможности)
- [Технологии](#-технологии)
- [Требования](#-требования)
- [Быстрый старт](#-быстрый-старт)
- [Настройка окружения](#-настройка-окружения)
- [Запуск с Docker](#-запуск-с-docker)
- [Ручная установка](#-ручная-установка)
- [Примеры запросов](#-примеры-запросов)
- [Структура проекта](#-структура-проекта)
- [Разработка](#-разработка)
- [Лицензия](#-лицензия)

## ✨ Возможности
- Анализ статистики видео на естественном языке
- Поддержка различных типов запросов (подсчет, сумма, фильтрация)
- Работа с историческими данными (почасовые срезы)
- Гибкая система фильтрации по датам и другим параметрам

## 🛠 Технологии
- **Python 3.9+**
- **PostgreSQL** - хранение данных
- **SQLAlchemy** - ORM
- **Alembic** - миграции базы данных
- **Docker** - контейнеризация
- **Docker Compose** - оркестрация сервисов
- **Aiogram** - фреймворк для Telegram ботов
- **OpenAI API** - обработка естественного языка

## 📋 Требования
- Docker и Docker Compose
- API ключ Telegram бота (от @BotFather)
- API ключ OpenAI (для работы с NLP)

## 🚀 Быстрый старт

### 1. Клонирование репозитория
```bash
git clone https://github.com/yourusername/TelBotRLTVideo.git
cd TelBotRLVideo
```

### 2. Настройка окружения
Скопируйте файл с примерами переменных окружения и отредактируйте его:
```bash
cp .env.example .env
```

### 3. Запуск с Docker (рекомендуется)
```bash
docker-compose up --build -d
```

### 4. Проверка работы
```bash
docker-compose logs -f app
```

## ⚙️ Настройка окружения
Перед запуском необходимо заполнить файл `.env`:

```env
# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=video_analytics
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# OpenAI
OPENAI_API_KEY=your_openai_api_key
```

## 🐳 Запуск с Docker

### Сборка и запуск
```bash
docker-compose up --build -d
```

### Остановка
```bash
docker-compose down
```

### Просмотр логов
```bash
docker-compose logs -f app
```

### Пересборка
```bash
docker-compose up --build -d --force-recreate
```

## 💻 Ручная установка

### 1. Установка зависимостей
```bash
python -m pip install -r requirements.txt
```

### 2. Настройка базы данных
1. Установите PostgreSQL
2. Создайте базу данных
3. Выполните миграции:
   ```bash
   alembic upgrade head
   ```

### 3. Загрузка данных
```bash
python scripts/load_data.py
```

### 4. Запуск бота
```bash
python main.py
```

## 💡 Примеры запросов

### Базовые запросы
- "Сколько всего видео в системе?"
- "Покажи статистику по видео с ID 123"
- "Сколько видео вышло за последнюю неделю?"

### Запросы по креаторам
- "Сколько видео у креатора с ID 456?"
- "Какой креатор имеет больше всего просмотров?"

### Аналитика просмотров
- "Какие видео набрали больше 1000 просмотров?"
- "Покажи динамику просмотров за последний месяц"
- "Какие видео получили больше всего лайков вчера?"

## 📁 Структура проекта

```
.
├── app/                    # Исходный код приложения
│   ├── __init__.py
│   ├── bot.py              # Основной файл бота
│   ├── database.py         # Настройки базы данных
│   ├── models.py           # Модели SQLAlchemy
│   └── services/           # Сервисные модули
│       └── nlp_service.py  # Обработка естественного языка
├── scripts/                # Вспомогательные скрипты
│   └── load_data.py        # Загрузка данных в БД
├── migrations/             # Миграции базы данных
├── .env.example            # Пример файла с переменными окружения
├── .dockerignore           # Игнорируемые файлы для Docker
├── docker-compose.yml      # Конфигурация Docker Compose
├── Dockerfile              # Конфигурация Docker
├── requirements.txt        # Зависимости Python
└── README.md               # Документация
```

## 🔧 Разработка

### Установка зависимостей разработки
```bash
pip install -r requirements-dev.txt
```

### Запуск тестов
```bash
pytest tests/
```

### Форматирование кода
```bash
black .
```

### Проверка стиля кода
```bash
flake8
```

### Создание миграций
```bash
alembic revision --autogenerate -m "Описание изменений"
```

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для получения дополнительной информации.

---

<div align="center">
  <p>Создано с ❤️ для аналитики видео</p>
  <p>2025</p>
</div>